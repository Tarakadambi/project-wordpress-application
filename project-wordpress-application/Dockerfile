# Use the official PHP + Apache image for WordPress
FROM wordpress:latest

# Copy custom files (if any) into the container
# COPY ./your-app-files /var/www/html/

# Set working directory
WORKDIR /var/www/html

# Expose the default port
EXPOSE 80
